# Terraform
.tfstate*
*.tfstate
*.tfstate.*
*.tfvars
.terraform/
terraform-gcp/.generated/
.generated/
laptop-vpn.conf

# GCP key for Terraform provider (never commit)
terraform-gcp/credentials.json
credentials.json

# Kubernetes / secrets
.kube/config
**/.kube/config

# WireGuard / SSH material (never commit)
WIREGUARD_CLIENT.local.txt
privatekey
*.pem
id_rsa
id_ed25519
